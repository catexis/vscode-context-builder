export const HARDCODED_EXCLUDES = ['node_modules/**', '.git/**', '.venv/**', '__pycache__/**', 'dist/**', 'build/**'];

export const CONFIG_PATH = '.vscode/context-config.json';

export const DEFAULT_DEBOUNCE_MS = 3000;
export const DEFAULT_MAX_FILE_SIZE_KB = 1024;
export const DEFAULT_MAX_TOTAL_FILES = 500;

// Memento Keys
export const KEY_ACTIVE_PROFILE = 'context-builder.activeProfile';
export const KEY_WATCHER_ENABLED = 'context-builder.watcherEnabled';
